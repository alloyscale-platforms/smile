services:
  postgres:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_USER: smile
      POSTGRES_PASSWORD: smile
      POSTGRES_DB: smile
    ports:
      - "5432:5432"
    volumes:
      - smile-postgres-data:/var/lib/postgresql/data

volumes:
  smile-postgres-data:
